# 蓝牙自动化测试平台概要设计文档

## 1. 项目概述

### 1.1 项目背景
开发一个运行在PC端的蓝牙自动化测试工具，通过WiFi ADB连接多个手机设备，对多个蓝牙设备进行自动化脚本测试。

### 1.2 测试对象
- **控制端**: PC端测试工具
- **被测设备**: 多个蓝牙设备（如耳机、音箱等）
- **执行端**: 多个Android手机（通过WiFi ADB连接）

### 1.3 技术栈
- **开发语言**: Python 3.8+ (PC端), Kotlin/Java (手机端APP)
- **架构模式**: CLI工具链为核心，GUI通过调用CLI实现功能
- **报告输出**: HTML、Excel
- **通信方式**: WiFi ADB、串口通信（AT指令）

---

## 2. 系统架构设计

### 2.1 整体架构图

```mermaid
flowchart TB
    subgraph PC["PC端测试平台"]
        GUI["GUI界面<br/>PySide6"]
        
        subgraph CLI_Tools["CLI工具链"]
            ADBCLI["ADB控制CLI"]
            SerialCLI["串口控制CLI"]
            LoggerCLI["日志收集解析CLI"]
            TestCLI["测试执行CLI"]
        end
        
        subgraph Core["核心引擎"]
            TM["测试管理器"]
            EM["执行引擎"]
            RM["结果管理器"]
        end
        
        subgraph Modules["功能模块"]
            ADBM["ADB管理"]
            SerialM["串口管理"]
            LoggerM["日志管理"]
            BTM["蓝牙控制"]
        end
        
        subgraph Data["数据层"]
            DB[(SQLite)]
            LogStore[(日志存储)]
            Config["配置文件"]
        end
    end
    
    subgraph Network["网络层"]
        WiFi["WiFi网络"]
    end
    
    subgraph Serial["串口层"]
        USB["USB串口"]
    end
    
    subgraph PhoneSide["手机端"]
        PhoneApp1["蓝牙测试APP"]
        PhoneApp2["蓝牙测试APP"]
        PhoneAppN["蓝牙测试APP"]
    end
    
    subgraph DUT["被测蓝牙设备"]
        BT1["蓝牙设备1"]
        BT2["蓝牙设备2"]
        BTN["蓝牙设备N"]
    end
    
    GUI -.->|调用| CLI_Tools
    
    CLI_Tools --> Core
    Core --> Modules
    Modules --> Data
    
    ADBM -->|ADB命令| WiFi
    WiFi -->|WiFi ADB| PhoneSide
    
    SerialM --> USB
    USB --> DUT
    
    PhoneSide -.->|蓝牙| DUT
    PhoneSide -.->|HCI日志| ADBM
    ADBM -->|传输HCI日志| LoggerM
    DUT -.->|模块日志| SerialM
    SerialM -->|传输模块日志| LoggerM
```

### 2.2 架构说明

**CLI工具链为核心**：所有业务功能通过CLI命令实现，各功能模块封装为独立CLI工具

**GUI为调用层**：GUI界面通过调用CLI命令实现功能，不直接操作核心模块

**手机端APP**：每个手机安装蓝牙测试APP，PC通过ADB命令与APP通信，间接控制手机蓝牙

---

## 3. CLI工具链设计

### 3.1 工具链组成

```mermaid
flowchart LR
    subgraph CLI["CLI工具链"]
        direction TB
        
        A["bt-adb<br/>ADB控制手机"]
        B["bt-serial<br/>串口控制模块"]
        C["bt-logger<br/>日志收集解析"]
        D["bt-test<br/>测试执行"]
    end
    
    A -->|控制| Phone["手机设备"]
    B -->|控制| Module["串口模块"]
    C -->|收集解析| Logs["各类日志"]
    D -->|调用| A
    D -->|调用| B
    D -->|调用| C
    D -->|生成| Report["测试报告"]
```

### 3.2 CLI工具清单

| 工具名称 | 功能 | 主要命令 |
|---------|------|---------|
| **bt-adb** | ADB控制手机 | list, connect, disconnect, status, command |
| **bt-serial** | 串口控制模块 | connect, send, sequence, interactive, status, start-log, stop-log, export-log |
| **bt-logger** | 日志收集解析 | start, stop, pull, parse, analyze, export |
| **bt-test** | 测试执行 | run, stop, status, prepare |

---

## 4. 数据流设计

### 4.1 GUI调用CLI数据流

```mermaid
sequenceDiagram
    actor User as 测试人员
    participant GUI as GUI界面
    participant CLI as CLI工具链
    participant Core as 核心引擎
    participant Device as 设备层

    User->>GUI: 操作界面
    GUI->>CLI: 调用CLI命令
    CLI->>Core: 调用核心功能
    Core->>Device: 操作设备
    Device-->>Core: 返回数据
    Core-->>CLI: 返回结果
    CLI-->>GUI: 返回输出
    GUI-->>User: 展示结果
```

### 4.2 PC与手机APP通信数据流

```mermaid
sequenceDiagram
    participant CLI as CLI工具
    participant ADB as ADB管理
    participant Phone as 手机APP
    participant BT as 手机蓝牙
    participant DUT as 被测设备

    CLI->>ADB: 发送ADB命令
    ADB->>Phone: am broadcast/start
    Phone->>Phone: 解析命令
    Phone->>BT: 调用Android API
    BT->>DUT: 蓝牙操作
    DUT-->>BT: 响应
    BT-->>Phone: 回调结果
    Phone-->>ADB: logcat输出
    ADB-->>CLI: 返回结果
```

### 4.3 测试执行数据流

```mermaid
flowchart TD
    A[GUI/CLI 发起测试] --> B[bt-test run]
    B --> C[加载测试配置]
    C --> D[bt-adb 获取设备]
    D --> E{设备就绪?}
    E -->|否| F[返回错误]
    E -->|是| G[执行测试前准备]
    
    G --> H[bt-serial 连接设备]
    H --> I[发送AT指令设置参数]
    I --> J{准备完成?}
    J -->|否| F
    J -->|是| K[并行启动]
    
    K --> L[bt-logger start --type hci]
    K --> M[bt-serial start-log]
    K --> N[执行测试脚本]
    
    L --> O[通过ADB采集HCI日志]
    M --> P[通过串口采集模块日志]
    N --> Q[通过APP执行蓝牙操作]
    N --> R[bt-serial 发送指令]
    
    Q --> S[收集结果]
    R --> S
    O --> T[停止日志]
    P --> T
    
    T --> U[bt-test 生成报告]
    U --> V[生成报告]
```

---

## 5. 模块设计（概要）

### 5.1 模块划分

```mermaid
flowchart TB
    subgraph PC_Modules["PC端模块"]
        A["ADB控制模块<br/>bt-adb"]
        B["串口控制模块<br/>bt-serial"]
        C["日志收集解析模块<br/>bt-logger"]
        D["测试执行模块<br/>bt-test"]
    end
    
    subgraph Phone_Modules["手机端模块"]
        PApp["蓝牙测试APP"]
    end
    
    A -->|ADB命令| Phone_Modules
    D -->|调用| A
    D -->|调用| B
    D -->|调用| C
    D -->|生成| Report["测试报告"]
    B -->|串口命令| BT["蓝牙设备"]
    C -->|采集解析| Logs["各类日志"]
    PApp -->|Android API| PhoneBT["手机蓝牙"]
    PhoneBT -.-> DUT["被测设备"]
```

### 5.2 模块职责

| 模块 | 职责 | 对外接口 |
|-----|------|---------|
| ADB控制 | 手机设备发现、连接、状态监控、执行ADB命令 | CLI命令 |
| 串口控制 | 串口设备连接、发送AT指令、接收响应、收集模块日志 | CLI命令 |
| 日志收集解析 | 通过ADB采集HCI日志、解析和分析各类日志 | CLI命令 |
| 测试执行 | 测试流程控制、任务调度、测试前准备（发送AT指令设置参数）、报告生成 | CLI命令 |
| 手机APP | 接收ADB命令，执行蓝牙操作，启用HCI日志 | ADB广播/Activity |

### 5.3 手机端APP功能

| 功能 | 说明 |
|-----|------|
| 蓝牙扫描 | 扫描周围蓝牙设备 |
| 配对管理 | 配对/取消配对 |
| 连接管理 | 连接/断开 |
| 数据传输 | 发送/接收数据 |
| HCI日志 | 启用/禁用btsnoop |
| 命令接收 | 通过BroadcastReceiver接收ADB命令 |
| 结果输出 | 通过logcat输出执行结果 |

---

## 6. 数据模型（概要）

### 6.1 核心实体

```mermaid
erDiagram
    DEVICE ||--o{ TEST_SESSION : participates
    DEVICE ||--o{ TEST_RESULT : generates
    TEST_SESSION ||--o{ TEST_RESULT : contains
    TEST_SESSION ||--o{ HCI_LOG : contains
    TEST_SESSION ||--o{ DEVICE_LOG : contains
    BLUETOOTH_DEVICE ||--o{ AT_COMMAND : executes
    
    DEVICE {
        string device_id
        string ip_address
        string status
    }
    
    BLUETOOTH_DEVICE {
        string mac_address
        string device_name
        string serial_port
    }
    
    TEST_SESSION {
        string session_id
        string status
        datetime start_time
        datetime end_time
    }
    
    TEST_RESULT {
        string result_status
        json test_data
        string log_path
    }
    
    HCI_LOG {
        string log_file_path
        datetime start_time
        datetime end_time
    }
    
    DEVICE_LOG {
        string log_file_path
        int line_count
    }
    
    AT_COMMAND {
        string command
        string response
        boolean success
    }
```

---

## 7. 报告设计

### 7.1 报告内容

| 报告格式 | 内容 |
|---------|------|
| **HTML** | 测试概览、详细结果、图表、日志链接、AT指令记录 |
| **Excel** | 多Sheet：汇总、详细结果、设备信息、日志摘要、AT指令记录 |

---

## 8. 接口设计

### 8.1 CLI接口（核心）

```bash
# ADB控制手机
bt-adb list
bt-adb connect <ip:port>
bt-adb disconnect <device_id>
bt-adb status <device_id>
bt-adb command <device_id> <adb_command>

# 串口控制模块
bt-serial connect --port <port> --baud <rate>
bt-serial send <command>
bt-serial sequence <file>
bt-serial interactive
bt-serial status

# 日志收集解析
bt-logger start --type hci --device <id>
bt-logger start --type device --port <port>
bt-logger stop
bt-logger pull --device <id>
bt-logger parse --file <log_file>
bt-logger analyze --file <log_file>
bt-logger export --format <format>

# 测试执行
bt-test run --config <file>
bt-test run --case <case_id>
bt-test stop
bt-test status
```

### 8.2 ADB与APP通信接口

```bash
# 启动APP
adb shell am start -n com.bt.test/.MainActivity

# 发送蓝牙扫描命令
adb shell am broadcast -a com.bt.test.SCAN

# 发送连接命令
adb shell am broadcast -a com.bt.test.CONNECT --es mac "xx:xx:xx:xx:xx:xx"

# 启用HCI日志
adb shell am broadcast -a com.bt.test.HCI_ENABLE

# 读取结果
adb logcat -d -s BT_TEST:V

# 拉取HCI日志
adb pull /sdcard/btsnoop_hci.log
```

### 8.3 GUI设计原则

- GUI通过调用CLI命令实现功能
- CLI返回JSON格式数据供GUI解析
- GUI提供可视化配置界面，生成CLI配置文件

---

## 9. 部署架构

```mermaid
flowchart TB
    PC["PC端<br/>Python + CLI工具链"] -->|WiFi ADB| Phone["手机集群<br/>蓝牙测试APP"]
    PC -->|USB串口| BT["被测蓝牙设备"]
    Phone -.->|蓝牙| BT
```

---

## 10. 技术选型

| 类别 | 技术/库 |
|-----|--------|
| CLI框架 | Click / Typer |
| GUI框架 | PySide6 |
| ADB控制 | adb-shell |
| 串口通信 | PySerial |
| 数据库 | SQLite |
| 报告生成 | Jinja2 + openpyxl |
| 手机端开发 | Kotlin + Android Bluetooth API |

---

## 11. 目录结构

```
bluetooth-test-platform/
├── cli/                    # CLI工具链
│   ├── bt-adb/             # ADB控制手机CLI
│   ├── bt-serial/          # 串口控制模块CLI
│   ├── bt-logger/          # 日志收集解析CLI
│   └── bt-test/            # 测试执行CLI
├── gui/                    # GUI界面
│   └── main.py             # 调用CLI实现功能
├── core/                   # 核心引擎（被CLI调用）
├── modules/                # 功能模块（被CLI调用）
│   ├── adb/                # ADB管理模块
│   ├── serial/             # 串口管理模块
│   ├── logger/             # 日志管理模块
│   └── test/               # 测试管理模块
├── models/                 # 数据模型
├── logs/                   # 日志存储
├── reports/                # 报告输出
├── config/                 # 配置文件
└── android-app/            # 手机端蓝牙测试APP
    ├── app/
    ├── src/
    └── build.gradle
```

---

## 12. 后续规划

1. **第一阶段**: 搭建CLI工具链框架、开发手机端APP基础功能
2. **第二阶段**: 测试执行CLI、HCI日志CLI、基础报告
3. **第三阶段**: 设备日志CLI、AT指令CLI、GUI界面
4. **第四阶段**: 高级报告、日志分析、性能优化
